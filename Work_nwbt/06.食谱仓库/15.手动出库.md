渲染出来查询到的数据的样式和效果与第一个文件不一致，然后前两个卡片是不动的，渲染的数据可滑动，然后我这样处理一下按钮，一开始用户扫描了标签渲染标签列表，点击确定后查询后渲染得到的数据，然后原本的确认按钮变为确认出库，再次点击确认出库后提示是否确定出库，点击确认后console一下出库的数据，后续编写请求接口



## BUG

这里面存在一个小问题，我先使用扫码或者扫描识别到标签后，点击确认，查询到商品后再切换为别的就有问题，这里需要调整一下，如果用户使用了某种方式，然后点击了确认之后，此时如果切换方式，需要提示先出库当前扫描到的商品或者先清空再切换，然后扫码和扫描本质上是一样的，通过标签列表获取数据的，因此这两种方式的渲染查询结果是一致的，提供修改后的完整js代码，如果其他部分有修改的地方，再单独给出


扫码扫到的也是tagList，它两的查询是一致的，请求的接口是一致的，所以返回的数据是一致的，渲染方式也一样，唯一的不同就是不需要点击确认再去查数据，如果连续扫描效果也是一样的，去除原有的扫码那种虚拟数据渲染，